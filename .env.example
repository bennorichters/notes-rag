# Server Environment Variables (for Dokku/VPS)
# Set these using: dokku config:set notes-rag KEY=value

# Required: Secret API key for authentication
API_KEY=your-secret-api-key-here

# Path to notes directory (will be mounted via Dokku storage)
NOTES_PATH=/app/notes

# Path to ChromaDB data (persistent storage)
CHROMA_PATH=/app/chroma_data
